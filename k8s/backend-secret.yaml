# Credenciais de desenvolvimento para cluster local (minikube/kind).
# Não usar estes valores em um cluster real/produção.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  labels:
    app: backend
type: Opaque
stringData:
  JWT_SECRET: dev-secret-troque-em-producao
