apiVersion: v1
kind: Service
metadata:
  name: projeto-nginx
  labels:
    app: projeto-nginx
spec:
  type: NodePort
  selector:
    app: projeto-nginx
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
